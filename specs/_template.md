# 仕様タイトル

| 項目 | 内容 |
| --- | --- |
| updated | YYYY-MM-DD |
| related | `docs/features.md` の該当章、関連仕様、関連画面 |

## 目的

この仕様で解決するユーザー課題と、アプリ上で実現する価値を書く。

## 対象ユーザー

- 対象レベル:
- 利用前提:

## ユーザーフロー

1. ユーザーがどこから開始するかを書く。
2. 主な操作を順番に書く。
3. 完了時に何が保存・表示・更新されるかを書く。

## 画面/状態

| 画面または状態 | 主アクション | 表示内容 | 遷移先 |
| --- | --- | --- | --- |
| 例: 初期状態 | 例: 始める | 例: 目的、所要時間、注意事項 | 例: 設問1 |

含める状態:

- ローディング
- 空状態
- 成功
- エラー
- オフライン
- 権限不足

## データ要件

| データ | 型/形式 | 必須 | 説明 |
| --- | --- | --- | --- |
| 例: targetLevel | `N3` / `N2` | yes | ユーザーの目標レベル |

Zod で検証する項目、初期値、更新タイミングを書く。

## API/Firebase 要件

| 種別 | パスまたは関数 | 読み書き | 説明 |
| --- | --- | --- | --- |
| Firestore | 例: `users/{userId}` | read/write | 例: ユーザープロフィール |

必要に応じて、React Query の query key、mutation、キャッシュ更新、権限ルールを書く。

## コンテンツ要件

問題、解説、音声、スクレイピング由来メタデータを扱う場合に書く。

- オリジナル作問の範囲:
- 参照するメタデータ:
- 禁止するコピー範囲:
- レビュー状態:

## エッジケース

- 未ログイン:
- データ未作成:
- 通信失敗:
- 途中離脱:
- 重複送信:
- 端末変更:

## 実装対象外

この仕様では実装しないことを書く。

- 未定

## 受け入れ条件

- [ ] 主要フローを完了できる。
- [ ] ローディング、空状態、エラー状態が表示される。
- [ ] `DESIGN.md` の画面設計ルールに従っている。
- [ ] 必要なデータが Zod で検証される。
- [ ] Firebase の読み書きと権限の前提が明記されている。
- [ ] 問題コンテンツを扱う場合、`docs/specs/content/question-content-policy.md` に従っている。

## 確認すべき質問

実装前に決める必要がある未確定事項を書く。

- 未定
